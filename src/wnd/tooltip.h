#ifndef _TOOLTIP_H
#define _TOOLTIP_H

#if 0
#include "../studio/services/svc_tooltips.h"

class Tooltip {

  public:

    Tooltip(const char *txt);
    virtual ~Tooltip();

  private:

    svc_toolTipsRenderer *svc;
};
#endif

#endif
